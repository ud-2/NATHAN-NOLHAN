# PYTHON - Sheet 4
**Total Questions:** 30 | **Duration:** 30m

### Section 1: Variables & Data Types
**1. Which of the following is a valid variable name in Python?**  
A) `1_number`  
B) `my-var`  
C) `_my_var`  
D) `class`

**2. What is the output of the function `type(3.14)`?**  
A) `<class 'int'>`  
B) `<class 'float'>`  
C) `<class 'str'>`  
D) `<class 'double'>`

**3. How do you correctly create a string variable?**  
A) `x = 'Hello'`  
B) `x = "Hello"`  
C) Both A and B are correct  
D) `string x = "Hello"`

**4. Which of these is a correctly formatted boolean value in Python?**  
A) `true`  
B) `TRUE`  
C) `True`  
D) `T`

**5. What is the result of the expression `int("25") + 5`?**  
A) `"255"`  
B) `30`  
C) Error  
D) `25.5`

### Section 2: Operators & Math
**6. What is the result of the expression `10 % 3`?**  
A) `3.33`  
B) `3`  
C) `1`  
D) `0`

**7. Which operator is used for exponentiation (power) in Python?**  
A) `^`  
B) `**`  
C) `//`  
D) `%`

**8. What does the `//` operator do?**  
A) Finds the remainder of a division  
B) Performs exact division  
C) Performs floor (integer) division  
D) Creates a one-line comment

**9. What is the output of `"Py" * 3`?**  
A) `"Py Py Py"`  
B) `"Py3"`  
C) `"PyPyPy"`  
D) Error

**10. Which operator is used to check if two values are equal?**  
A) `=`  
B) `==`  
C) `!=`  
D) `is_equal`

### Section 3: Input & Output (I/O)
**11. What data type does the `input()` function ALWAYS return by default?**  
A) Integer  
B) Float  
C) Boolean  
D) String

**12. How would you output the exact text "Hello World" to the screen?**  
A) `print(Hello World)`  
B) `print("Hello World")`  
C) `echo "Hello World"`  
D) `output("Hello World")`

**13. What is the output of `print("A", "B", sep="")`?**  
A) `A B`  
B) `A, B`  
C) `AB`  
D) `"A" "B"`

### Section 4: Conditions (If/Elif/Else)
**14. Which statement correctly starts an `if` block?**  
A) `if x > 5`  
B) `if (x > 5)`  
C) `if x > 5:`  
D) `if x > 5 then:`

**15. What keyword is used to check an additional condition if the first `if` is False?**  
A) `else if`  
B) `elseif`  
C) `elif`  
D) `then`

**16. What is the evaluation of this expression: `(5 > 2) and (3 > 8)`?**  
A) `True`  
B) `False`  
C) Error  
D) `None`

**17. How does Python know which lines of code belong inside an `if` block?**  
A) By using curly braces `{}`  
B) By using parentheses `()`  
C) By using indentation (spaces/tabs)  
D) By using semicolons `;`

### Section 5: Loops (For/While)
**18. How many times will the loop `for i in range(4):` execute?**  
A) 3  
B) 4  
C) 5  
D) Infinite

**19. Which keyword is used to exit a loop entirely before it finishes?**  
A) `stop`  
B) `exit`  
C) `continue`  
D) `break`

**20. Which keyword skips the rest of the current loop iteration and immediately starts the next one?**  
A) `skip`  
B) `pass`  
C) `continue`  
D) `break`

**21. What sequence of numbers will `range(1, 4)` generate?**  
A) `1, 2, 3, 4`  
B) `0, 1, 2, 3`  
C) `1, 2, 3`  
D) `2, 3, 4`

**22. What is the most common cause of an infinite `while` loop?**  
A) Using the `break` keyword  
B) The loop's condition never becomes `False`  
C) Using the `continue` keyword  
D) The loop's condition never becomes `True`

**23. If `colors = ["red", "blue"]`, what does `for c in colors:` do?**  
A) Loops through the numbers 0 and 1  
B) Loops through the strings "red" and "blue" directly  
C) Causes an error because `range()` is missing  
D) Loops through the individual letters 'r', 'e', 'd', 'b', 'l', 'u', 'e'

### Section 6: Simple Lists
**24. In a Python list, what is the index of the very first element?**  
A) `1`  
B) `-1`  
C) `0`  
D) It depends on the size of the list

**25. Which list method is used to add an item to the very end of a list?**  
A) `add()`  
B) `insert()`  
C) `push()`  
D) `append()`

**26. How do you find the total number of elements in a list called `my_list`?**  
A) `my_list.length()`  
B) `size(my_list)`  
C) `len(my_list)`  
D) `count(my_list)`

**27. What does `my_list[-1]` return?**  
A) The first element  
B) The last element  
C) An error  
D) The second element

### Section 7: Functions
**28. Which keyword is used to define a custom function in Python?**  
A) `function`  
B) `def`  
C) `define`  
D) `func`

**29. How do you send a value back from a function to the place where it was called?**  
A) `send`  
B) `output`  
C) `return`  
D) `yield`

**30. If you define a function as `def greet(name):`, what is the correct way to call it?**  
A) `call greet("John")`  
B) `greet("John")`  
C) `greet "John"`  
D) `def greet("John")`
